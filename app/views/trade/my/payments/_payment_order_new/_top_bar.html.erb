<div class="as-bar">
  <%= link_to({ controller: 'orders', action: 'show', id: @payment_order.order_id }, class: 'left') do %>
    <%= svg_tag 'angle-left', kind: 'regular' %>
  <% end %>
  <div class="bar-title"><%= @payment_order.order.uuid %></div>
  <i class="right"></i>
</div>

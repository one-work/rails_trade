<div class="as-bar">
  <%= link_to({ controller: 'orders', action: 'payment_types', id: @order.id }, class: 'left') do %>
    <%= svg_tag 'angle-left', kind: 'regular' %>
  <% end %>
  <div class="bar-title"><%= @order.uuid %></div>
  <i class="right"></i>
</div>

<div class="as-bar">
  <%= link_to({ controller: 'orders', action: 'show', id: @item.order_id }, class: 'left') do %>
    <%= svg_tag 'angle-left', kind: 'regular' %>
  <% end %>
  <div class="bar-title"><%= @item.order.uuid %></div>
  <i class="right"></i>
</div>

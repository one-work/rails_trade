<div class="card">
  <div class="card-header flex-direction-column">
    <div class="has-block-space-between flex-grow-1 px-3 py-2">
      <%= link_to({ controller: 'orders', action: 'show', id: model.id }, class: 'has-block-left') do %>
        <time data-controller="time" data-format="HH:mm" class="text-weight-bold"><%= model.created_at.to_fs(:iso8601) %></time>
        <% if model.agent %>
          <div class="tags has-addons mb-0">
            <span class="tag is-info">下单人</span>
            <span class="tag is-link"><%= model.agent.name %></span>
          </div>
        <% end %>
        <span class="tag"><%= model.payment_status_i18n %></span>
      <% end %>
      <%=
        button_to(
          { controller: 'print/admin/home', action: 'inner' },
          params: { gid: model.to_gid.to_s, aim: 'receipt' },
          id: 'print-button',
          data: { controller: 'print-pos' }
        ) do
      %>
        <%= svg_tag 'print', class: 'icon is-small text-link' %>
      <% end %>
    </div>
    <div class="has-block-space-between flex-grow-1 px-3 mb-2">
      <% if model.print_info.key?('dine_prepare') %>
        <div>
          <span><%= Trade::Order.enum_i18n(:print_info_enum, 'dine_prepare') %></span>
          <span>：</span>
          <time data-controller="time" data-format="human" class="text-grey"><%= model.print_info['dine_prepare'] %></time>
        </div>
        <%=
          button_to(
            { controller: 'print/admin/home', action: 'inner' },
            params: { gid: model.to_gid.to_s, aim: 'dine_prepare' },
            id: 'print-button',
            data: { controller: 'print-pos' }
          ) do
        %>
          <%= svg_tag 'print', class: 'icon is-small text-link' %>
        <% end %>
      <% else %>
        <%=
          button_to(
            { controller: 'print/admin/home', action: 'inner' },
            params: { gid: model.to_gid.to_s, aim: 'dine_prepare' },
            id: 'print-button',
            data: { controller: 'print-pos' },
            class: 'button is-small is-rouned is-primary'
          ) do
        %>
          <%= svg_tag 'print' %>
          <span>打印备菜单</span>
        <% end %>
      <% end %>
    </div>
  </div>
  <%= render layout: 'index_list', locals: { order: model } do %>
    <%= render partial: 'index_item', layout: 'index_item_actions', collection: model.items, as: :model %>
  <% end %>
</div>

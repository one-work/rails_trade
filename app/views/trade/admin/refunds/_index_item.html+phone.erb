<p>
  <span><%= link_to model.payment.payment_uuid, { controller: 'payments', action: 'show', id: model.payment_id } %></span>
</p>
<p>
  <span class="tag is-info"><%= model.type_i18n %></span>
  <span class="tag"><%= model.state_i18n %></span>
</p>
<p><%= model.total_amount.to_money(model.currency).format.html_safe %></p>
<p class="text-grey">
  <time data-controller="time" data-format="human"><%= model.refunded_at.to_fs(:iso8601) if model.refunded_at %></time>
</p>
